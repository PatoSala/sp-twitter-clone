async function uploadBanner(path) {
    let url = 'https://serysjohsewrcxkonnum.supabase.co/storage/v1/object/banners/' + session.user_id;
    let formData = new FormData();
    formData.append()
    let response = await fetch(url, {

    });
}